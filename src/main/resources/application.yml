spring:
  application:
    name: secure_blog_management

  datasource:
    url: jdbc:postgresql://localhost:5432/secure_blog_db
    username: postgres
    password: qweasdzxc
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

server:
  port: 8080


---

